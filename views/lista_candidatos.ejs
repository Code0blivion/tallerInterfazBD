<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Candidatos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="table-styles.css">
</head>

<body class="container text-center text-bg-info mb-3">

    <section>
        <table id="cand-reg-table" class="table table-hover table-bordered align-middle">
            <thead>
                <tr class="table-primary ">
                    <th>ID</th>
                    <th>Usuario</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Fecha de Nacimiento</th>
                    <th>Número de documento</th>
                    <th>Tipo de documento</th>
                </tr>
                <% for(let i=0; i < candidatos.length; i++) { %>
                    <tr>
                        <td>
                            <%=candidatos[i].id%>
                        </td>
                        <td>
                            <%=candidatos[i].usuario%>
                        </td>
                        <td>
                            <%=candidatos[i].nombre%>
                        </td>
                        <td>
                            <%=candidatos[i].apellido%>
                        </td>
                        <td>
                            <%=candidatos[i].fecha%>
                        </td>
                        <td>
                            <%=candidatos[i].numDoc%>
                        </td>
                        <td>
                            <%=candidatos[i].tipoDoc%>
                        </td>
                    </tr>
                    <% } %>
            </thead>

            <tbody></tbody>
        </table>
        <button id="back-button" class="btn btn-light">Regresar al Inicio</button>
    </section>

    <script>
        document.getElementById('back-button').addEventListener('click', (e) => {
            e.preventDefault();
            location.href = "/";
        })
    </script>

</body>

</html>